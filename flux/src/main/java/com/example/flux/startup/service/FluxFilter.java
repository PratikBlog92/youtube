package com.example.flux.startup.service;

import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;
import reactor.core.publisher.Flux;

import java.util.Arrays;
import java.util.List;

@Component
public class FluxFilter implements CommandLineRunner {

    public void run(String... args) throws Exception {
//        Flux<String> stringFlux = Flux.just("Code", "With", "Pratik");
//        stringFlux.filter(x-> x.length() > 4).log().subscribe();
//
//        Flux<Integer> integerFlux = Flux.just(2, 3, 4, 5, 6, 7, 8, 9, 10);
//        integerFlux.filter(x-> x%2==0 ).log().subscribe();

    }
}
